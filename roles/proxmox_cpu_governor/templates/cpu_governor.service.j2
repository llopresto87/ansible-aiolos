[Unit]
Description=Set CPU governor to {{ proxmox_cpu_governor_governor }}

[Service]
ExecStart=/bin/bash -c 'for gov in /sys/devices/system/cpu/cpu*/cpufreq/scaling_governor; do echo {{ proxmox_cpu_governor_governor }} > $gov; done'
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
